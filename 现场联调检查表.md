# 现场联调检查表

## SA 端

- 确认 SA 2025 可以在内部正常运行 MP。
- 将“静态点集导出”和“Moving 单点导出”分配到 Quick MP/自定义工具栏。
- 两个导出命令的 `Append` 均为 `FALSE`。
- Moving 点组每次只包含当前一个点。
- Moving MP 的输出路径与桥接程序界面设置完全一致。
- 先用五个已知测试点核对 ASCII 列顺序和小数精度。
- 在真实ATS600连接后，打开仪器接口并查找 `Utility > UDP Data Stream`。
- 开启 `3D Updates`，目标端口与桥接程序保持一致（默认10000）。
- 保存10～50行原始UDP数据，并记录同一时刻SA显示的X/Y/Z。
- 核对UDP是否为毫米和测量坐标系绝对位置。
- 查找 `Watch Update` 配置，记录 Acquisition、Operation、Sampling Frequency 和丢光行为。

## TCP 端

- 确认服务器地址和端口。
- 确认命令是 UTF-8 JSON，并以 `%` 结束。
- 分别测试粘连的两条命令和被拆成两次发送的一条命令。
- 测试服务器断开时界面能显示“未连接”。

## 协议顺序

- `HTDJ/Init`：接受后 `working`，加载点集后 `InitOK`。
- `HTDJ/Move`：每次 Moving 文件覆盖后自动发送一个 `Moving`。
- `HTDJ/Move`：实时UDP有效时，应按默认10Hz发送最新坐标；数据断流300ms后必须暂停。
- `HTDJ/Stop`：自动发送 `finish`。
- 对 `SQLW` 的每个分组分别加载文件，检查 JSON 分组名称。
- 对 `GJZP` 的每个分组分别加载文件，并测试 `BottomHeight`。
- 对 `ZCTP/Start` 重点确认上位机期望：当前版本为接受时发送 `working`，点集发送时发送带点的 `finish`。

## 必做异常测试

- Moving 文件有零个或两个点时不得发送。
- Move 命令前已经存在的旧 Moving 文件不得发送。
- 同一文件未变化时不得重复发送。
- UDP发送高于10Hz时，TCP Moving输出不得超过配置频率且不得积压旧点。
- UDP停止超过300ms后不得继续重复发送最后一点。
- HTDJ 缺少或使用非法 `Component` 时拒绝命令。
- TCP 未连接时点击发送必须提示失败。
