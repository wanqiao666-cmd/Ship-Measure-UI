# SA 测量通信桥接程序 V2 初版

## 功能范围

本程序在安装 SpatialAnalyzer 的同一台 Windows 电脑上运行，但不调用 SA SDK。

- 作为 TCP 客户端连接上位机服务器。
- 使用 `%` 作为每条 JSON 报文的结束符。
- 支持 `HTDJ`、`ZCTP`、`SQLW`、`GJZP`。
- `Init/Start` 到达后，由操作员点击“接受任务并开始测量”发送 `working`。
- 静态点集由操作员加载 SA 导出的 ASCII 文件并点击发送。
- `Move` 到达后，程序优先接收 ATS600/SA 的实时 UDP 坐标，并按设置频率发送 `Moving`。
- 旧版 `moving.txt` 监视仍作为无跟踪仪环境下的备用测试方式保留。
- 收到 `Stop` 后自动回复 `finish`。

## 启动

直接运行：

```text
SA_Measure_Bridge.exe
```

开发环境也可以运行：

```text
python main.py
```

服务器地址、端口和 Moving 文件路径保存在当前 Windows 用户的：

```text
%APPDATA%\SAMeasureBridge\config.json
```

日志位于：

```text
%APPDATA%\SAMeasureBridge\logs\
```

## 上位机命令格式

目前按以下格式实现：

```json
{"Cmd":"Measure","Params":{"Stage":"HTDJ","Component":"Leg1","State":"Init"}}%
```

支持的 State：`Init`、`Start`、`Move`、`Stop`。

`HTDJ` 要求 `Component` 为 `Leg1`～`Leg4`。其他阶段允许不发送 `Component`。

## 静态测量操作

1. 上位机发送 `Init` 或 `Start`。
2. 程序显示阶段、部件和命令。
3. 操作员点击“接受任务并开始测量”，程序发送 `working`。
4. 操作员在 SA 中手动测量。
5. 在 SA 中运行静态点导出 MP，或手工导出 ASCII 文件。
6. 在程序中选择数据组，点击“加载 ASCII 文件”。
7. 检查表格中的点名和坐标。
8. 点击“发送静态测量结果”。

`HTDJ` 和 `ZCTP` 使用统一的 `Points` 数据组。

`SQLW` 支持：

```text
Top_Mark_GJ
Top_Mark_CL
Center
Target
```

`GJZP` 支持：

```text
Top_Mark_GJ
Top_Mark_CL
Bottom_Mark_GJ
Bottom_Mark_CL
Center
Target
BottomHeight（可选数值）
```

每个数据组可以分别加载一个 ASCII 文件；`Num` 根据实际读取的点数自动生成，不固定点数。

## Moving 操作

1. 在桥接程序中启动 UDP 监听，默认监听 `0.0.0.0:10000`。
2. 上位机发送 `Move`。
3. 操作员在 SA/ATS600 仪器接口中启动连续位置更新。
4. 桥接程序接收最新坐标，并按默认 `10 Hz` 上限发送 `Moving`。
5. UDP 数据超过 `300 ms` 未更新时，程序自动暂停发送旧坐标。
6. 上位机发送 `Stop` 后，程序立即停止 Moving 发送并回复 `finish`。

桥接程序采用“最新值优先”，不会将高频 UDP 包排成长队发送。对于约 `2 mm/s` 的移动速度，10 Hz 对应约 `0.2 mm` 的位置间隔。

### V2 初版对 ATS600 UDP 的暂定假设

由于尚未取得 SA 2025.2 + ATS600 的真实 UDP 样本，解析器暂时兼容以下最可能格式：

```text
ATS600 X=1000.123456 Y=2000.123456 Z=3000.123456
```

```text
ATS600 1000.123456 2000.123456 3000.123456
```

```json
{"instrument":"ATS600","x":1000.123456,"y":2000.123456,"z":3000.123456}
```

对普通 ASCII，解析器取最后三个有效数值作为 X、Y、Z。实际 UDP 数据取得后，必须把原始样本与 SA 同时刻显示坐标进行核对，再确认解析规则。

### UDP模拟器

没有 ATS600 时，可先启动桥接程序的 UDP 监听，再运行：

```text
python ats600_udp_simulator.py
```

模拟器以20 Hz发送连续坐标，其中X轴按约 `2 mm/s` 移动。

## SA ASCII 文件格式

程序兼容空格、Tab 或逗号分隔，并把每个有效数据行的最后三个数值解析为 X、Y、Z：

```text
Loc_1   9.831564   -7.062013   3.314349
Loc_2   -21.983732   -6.954340   -4.467675
```

支持 UTF-8、GB18030 和 UTF-16 文本。空行、标题行以及以 `#`、`;`、`//` 开头的注释行会被忽略。

## SA 2025 MP 建议配置

在 SA 内建立两个 MP，均只使用 `Export ASCII Points`，不通过 SDK 启动。

### 静态导出 MP

- File Path：固定的静态文件路径。
- Group Names to export：由操作员选择当前点组，或约定固定点组。
- Data Delimiter：Space 或 Tab。
- Target Name Format：确保输出点名。
- Desired Coordinate System：当前阶段需要的坐标系；现场建立坐标系后配置。
- Include Target Offsets：FALSE。
- Include Target Comments：FALSE。
- Include Timestamps：FALSE。
- Include Tolerances：FALSE。
- Maximum Precision：FALSE。
- Decimal Precision：6 或现场要求的精度。
- Append：FALSE，必须覆盖旧文件。

### Moving 导出 MP（旧版备用）

参数相同，但：

- 固定导出只包含当前一个点的点组。
- File Path 必须与桥接程序设置的 Moving 文件完全一致。
- Append 必须为 FALSE。

将两个 MP 分配到 SA 的 Quick MP，并添加到 SA 自定义工具栏，即可在 SA 中一键执行。

## 测试服务器

开发联调时先运行：

```text
python test_server.py
```

桥接程序连接 `127.0.0.1:12345` 后，在测试服务器输入：

```text
HTDJ Leg1 Init
HTDJ Leg1 Move
HTDJ Leg1 Stop
SQLW - Init
GJZP - Init
ZCTP - Start
```

## 当前约定

- `Init`：接受后发送 `working`，发送静态结果时使用 `InitOK`。
- `Start`：接受后发送 `working`，发送静态结果时使用 `finish` 并携带点集。
- `Move`：优先以实时 UDP 坐标持续发送 `Moving`；新单点文件仍可用于备用测试。
- `Stop`：立即自动发送 `finish`。
- 不向协议添加 RequestId、时间戳、序号或其他字段。

现场联调时如果上位机的 `Start/ZCTP` 完成报文规则不同，只需调整协议生成规则，不影响界面、文件解析和 TCP 部分。
